server:
  port: 8085

spring:
  application:
    name: wallet-service
#  profiles:
#    active: local

  datasource:
    url: jdbc:h2:mem:walletDB
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

api:
  gateway:
    base-url: http://localhost:8080/wallet-service

security:
  jwt:
    secret: ${JWT_SECRET:y5x8ZrQF0V6pN8yM7A6e5zT2B3C1XvKpLkU2nH9Jc0A=}
