apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: backend
spec:
  clusterIP: None
  selector:
    app: kafka
  ports:
  - name: client
    port: 9092
    protocol: TCP
    targetPort: 9092

