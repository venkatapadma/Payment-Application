server:
  port: 8080

spring:

  application:
    name: api-gateway-service

  httpclient:
    max-initial-line-length: 4096
    max-header-size: 8192
  filters:
    - name: RequestSize
      args:
        maxSize: 1MB

security:
  jwt:
    secret: ${JWT_SECRET:y5x8ZrQF0V6pN8yM7A6e5zT2B3C1XvKpLkU2nH9Jc0A=}
